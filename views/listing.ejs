<!DOCTYPE html>
<html lang="en" data-bs-theme="dark">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Uploaded Books</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <style>
        body {
            background-color: #212529;
            color: #ffffff;
            font-family: Arial, sans-serif;
        }
        .container {
            margin-top: 50px;
            margin-bottom: 50px;
        }
        .book-card {
            background-color: #343a40;
            border-radius: 8px;
            margin-bottom: 1.5rem;
            padding: 1rem;
            display: flex;
            align-items: flex-start;
        }
        .book-card img {
            width: 100px;
            height: 150px;
            object-fit: cover;
            border-radius: 8px;
        }
        .book-details {
            flex: 1;
            padding-left: 1rem;
        }
        .book-details h3 {
            margin-bottom: 0.5rem;
        }
        .book-details .date {
            font-size: 0.875rem;
            color: #adb5bd;
        }
        .book-details .summary {
            margin-top: 0.5rem;
        }
        /* Modified color class with !important */
        .color-blue {
            color: #6ea8fe !important; /* Blue color */
        }
        .btn-container {
            display: flex;
            gap: 0.5rem;
            margin-top: 1rem;
        }
    </style>
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container-fluid">
            <a class="navbar-brand" href="#">Book Buddy</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                    <li class="nav-item"><a href="/secrets" class="nav-link active" aria-current="page">Home</a></li>
                    <li class="nav-item"><a href="/about" class="nav-link">About</a></li>
                </ul>
            </div>
        </div>
    </nav>
    <div class="container">
        <% if (books.length > 0) { %>
            <div class="row">
                <% books.forEach(function(book) { %>
                    <div class="col-md-6">
                        <div class="book-card">
                            <img src="/<%= book.book_img_location %>" alt="Book Image">
                            <div class="book-details">
                                <strong class="d-inline-block mb-2 text-primary-emphasis"><%= book.type %></strong>
                                <h3 class="mb-0"><%= book.title %></h3>
                                <div class="date mb-1"><%= new Date(book.date).toLocaleDateString('en-GB', { day: 'numeric', month: 'short', year: 'numeric' }) %></div>
                                <p class="summary"><%= book.summary %></p>
                                <div class="btn-container">
                                    <a href="/bookdata/<%= book.id %>"  class="text-white color-blue">Continue reading <i class="fas fa-chevron-right"></i></a>
                                    <a href="/updatebook/<%= book.id %>" class="btn btn-warning btn-sm">Update</a>
                                    <form action="/removebook/<%= book.id %>" method="POST" style="display: inline;">
                                        <button type="submit" class="btn btn-danger btn-sm">Remove</button>
                                    </form>
                                </div>
                            </div>
                        </div>
                    </div>
                <% }); %>
            </div>
        <% } else { %>
            <p>No books are listed by You.</p>
        <% } %>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-kenU1KFdBIe4zVF0s0G1M5b4hcpxyD9F7jL+DY8nB6u5W8KdJ6fHe9KN4zrF4Q05" crossorigin="anonymous"></script>
</body>
</html>
